# The WebMCP Challenge — Requirements Checklist

Status: no submission has been sent. Every unchecked required or conditional item is an open release gate.

**The submission deadline (`2026-09-03T20:00:00Z`) has already passed as of this audit (2026-09-05, confirmed by both the environment clock and `date` in this session). Nothing below changes that fact; see "Submit and preserve" for where this is recorded on the record itself.**

Official data was fetched from the authenticated Devpost integration on 2026-08-27 UTC. Recheck the [challenge](https://webmcp.devpost.com/), [rules](https://webmcp.devpost.com/rules), and live form before submission. Complete the [shared release checklist](../shared-release-checklist.md) as well.

Deadline: September 3, 2026 at 1:00 p.m. PT / 4:00 p.m. ET (`2026-09-03T20:00:00Z`).

## Eligibility and registration — human verification required

- [ ] The submitter is registered for The WebMCP Challenge. — submitter action; no code or document in this repository can attest to a Devpost account's registration status.
- [ ] The submitter and every team member are above the legal age of majority in their country of residence. — submitter action; a personal/legal attestation.
- [ ] No participant resides in an excluded country or territory listed by the current official rules. — submitter action; only the submitter can attest current residency against the live rules page.
- [ ] The submitter has reviewed the complete official rules, not only the eligibility summary. — submitter action.
- [ ] Submitter type is chosen truthfully as Individual, Team of Individuals, or Organization. — submitter action (Devpost field `28249`).
- [ ] Country or countries of residence are entered truthfully. — submitter action (Devpost field `28250`).
- [ ] Organization name is supplied only if applicable. — submitter action (Devpost field `28251`).
- [ ] Team membership and invitations are complete before submission. — submitter action; `git log` in this repository shows a single author (Jordan Allen) and no team-invitation artifact exists to check.

## Application qualification

- [x] Sift is submitted as `New` only if the final repository history supports that answer. — `git log --reverse --format='%h %ad %s' --date=iso | head -3` shows the repository's first commit is `2008a9c` at `2026-08-27 08:31:57 -0400`, after the official submission window opened (August 25, 2026 at 12:00 p.m. PT) and inside it; `LICENSE` carries a fresh 2026 Jordan Allen copyright, and `docs/reuse-attribution.md` documents only small, individually attributed reused pieces (confirmed present and non-trivial by `pnpm test:submission`'s `fixture-attribution` check in the current `release-latest` report). `New` is the factually supported answer.
- [ ] If Sift is submitted as `Existing`, the form precisely describes meaningful WebMCP work completed during the official submission period beginning August 25, 2026 at 12:00 p.m. PT. — not applicable; the item directly above establishes `New` is the correct, evidence-backed answer, so this conditional field (Devpost field `28253`, per `submission-details.md`'s field table: "Not applicable if `New`") never activates. Left unticked as a genuine N/A rather than ticked as vacuously true, since the live form has not actually recorded either answer yet.
- [x] The submitted implementation is a working web application, not a concept, mockup, or video-only prototype. — the live deployment answers real HTTP traffic verified in this session (`curl https://sift-hackathon-production.up.railway.app/` → `200`, real `index.html`; `.../ping` → `{"status":"Healthy",...}`), backed by a real migrated SQLite store and a passing `test:integration`/`test:e2e` suite (`artifacts/verification/latest/report.json`, gitSha `3ee4069`, `status: "passed"`, all ten stages including `test:e2e` passed).
- [x] The use case demonstrates people and agents interacting, collaborating, or creating together through the live web application. — `artifacts/host-acceptance/2026-09-02T18-26-35-407Z/report.json` (a real Chrome 152 session against the live deployed URL, 14/14 checks passing) shows both directions live: check 7 "a person clicks in the pane; the host's next read reflects it" (`eventSequence` 12 → 13) and check 9 "the host writes; the pane renders it without a reload."
- [x] The public implementation contains a real `document.modelContext.registerTool(...)` integration. — `apps/web/src/model-context/adapter.ts` line 109: `await document.modelContext!.registerTool(definition, options);` inside the real, non-test `BrowserModelContextAdapter` (its `supported()` check at line 96 gates on `typeof document.modelContext?.registerTool === 'function'`); this is what the live host-acceptance session (check 1, "host surface — pass — document.modelContext with registerTool() is present") actually exercised against the deployed page, not a mock.
- [x] Every claimed WebMCP tool is registered by the deployed page and has a bounded, typed contract. — `apps/web/src/model-context/webmcp-contract.test.ts` "registers exactly the twenty-six catalog tool names this module defines, no more and no fewer" pins `SIFT_WEBMCP_TOOL_NAMES` at exactly 26 and asserts every tool's `inputSchema` equals `z.toJSONSchema(sourceSchema)` for a real Zod schema (e.g. `GetCaseContextInputSchema = z.object({}).strict()` in `packages/contracts/src/commands.ts`); the live host session's "discovery (case open)" check independently confirms 26 tools registered against the deployed page with real JSON schemas reaching the host.
- [x] The page works in ChatGPT's in-app browser or a compatible Google Chrome configuration with WebMCP enabled. — the "or" is satisfied by the Chrome branch: `artifacts/host-acceptance/2026-09-02T18-26-35-407Z/report.json` records `browserVersion: "152.0.7977.75"` driven natively (no polyfill) over the CDP `WebMCP` domain against the live deployed URL, 14/14 checks passing. The ChatGPT branch has not been exercised (see the "Genuine gaps" note under "WebMCP functionality proof" and item 138 below for exactly what remains).

## Required live application

- [x] A judge-accessible public live URL is available. — verified live in this session: `curl -s -o /dev/null -w '%{http_code}' https://sift-hackathon-production.up.railway.app/` → `200`.
- [x] The URL loads without private-network access, local setup, or participant credentials. — the `curl` calls above used no auth headers, cookies, or VPN and succeeded; `apps/agent/src/app.ts` and `apps/agent/src/server.ts` contain no authentication/authorization middleware (`grep -rn "Authorization\|apiKey\|requireAuth\|basicAuth"` over both files returns nothing).
- [x] Any unavoidable credentials or setup steps are supplied in the testing instructions. — vacuously true (no credentials are needed, per the item above) and `docs/submissions/webmcp/submission-details.md` "Testing instructions draft" gives a concrete, credential-free seven-step sequence starting from "Open the public URL."
- [ ] The deployed URL maps to the final public repository commit. — genuinely not true right now: `docs/submissions/release-metadata.json`'s `railway.latestDeploymentGitSha` is `73d60b1`, confirmed by commit `3ee4069`'s own message ("The public URL now runs 73d60b1. Verified live... POST /api/cases/energy-bill-feed-check returns caseOpened:true..."); current `HEAD` is `05fb5d3`, two commits ahead (`3ee4069`, a metadata-only commit, and `05fb5d3`, a test-only mutation-kill fix — `git log --oneline 73d60b1..HEAD` confirms exactly these two, neither touching product behavior). A redeploy is needed before the deployed URL genuinely matches the commit that would be submitted.
- [x] The live right-pane UI is usable at the target ChatGPT browser-pane width. — `playwright.config.ts`'s `chatgpt-pane-640` project (640×900) exists specifically for this: its own comment reads "ChatGPT's own side pane, and the reason this project exists... The single most important viewport this product has was in that gap the whole time"; `tests/e2e/car-purchase-journey.spec.ts-snapshots/` contains passing baselines at that exact project name (36 PNGs total = 6 states × 6 viewport projects, `chatgpt-pane-640` among them, confirmed by directory listing).
- [x] The live demo can be reset to a deterministic starting state. — `apps/agent/src/services/command-service.ts`'s `startDemo` seeds every field of a fresh case (title, criteria, obligations, entities) from the pinned pack's fixture data with a freshly generated case id each call (confirmed by reading the seeding block, lines ~350-380); the README states this directly: "This starts the car-purchase case from its checked-in fixture — a fresh case ID every time."
- [ ] The URL and full hero flow are verified in an incognito window. — not verified through an actual browser-based incognito session in this pass. The `curl` checks above are unauthenticated (functionally similar to signed-out access) but are not a literal incognito-window verification of the full hero flow.

## Required public repository

- [x] A public GitHub, GitLab, or Bitbucket URL is supplied. — `gh repo view jordanallen87/sift --json visibility,licenseInfo,url` (run in this session) returns `{"url":"https://github.com/jordanallen87/sift","visibility":"PUBLIC",...}`.
- [x] The repository includes complete source, assets, setup instructions, and run instructions. — `README.md` is 726 lines with a Quickstart, both demos, a dedicated WebMCP section, Architecture, Testing, and Deployment; `pnpm test:submission`'s `required-files` check (in the current `release-latest` report) passed all 9 required submission files present.
- [x] An open-source license is present at the repository root and visible at the top of the repository page. — `LICENSE` at the repo root is real MIT text ("MIT License / Copyright (c) 2026 Jordan Allen", confirmed by reading it directly); `gh repo view` reports `"licenseInfo":{"key":"mit","name":"MIT License"}`, which is what GitHub surfaces at the top of a public repository's file listing/About sidebar.
- [x] The repository visibly contains the WebMCP registration and tool implementations. — `apps/web/src/model-context/register-sift-tools.ts` (1,491 lines) and `apps/web/src/model-context/adapter.ts` are committed, ordinary source files at an obvious, discoverable path, not hidden or gitignored.
- [x] The README explains how to enable and test WebMCP in each supported client. — README's `## WebMCP` section (line 234 onward) gives both clients explicitly: "1. ChatGPT's WebMCP-capable in-app browser... 2. Chrome 152 or newer, which ships WebMCP natively... `--enable-features=WebMCP,WebMCPTesting,DevToolsWebMCPSupport`," plus `pnpm test:host` and `pnpm webmcp:bridge` (a stdio MCP server bridging any MCP client to the page's live WebMCP registrations) as concrete testing tools.
- [ ] A clean clone can install, build, test, and run using the documented commands. — not independently re-verified in this pass: this audit's instructions explicitly exclude `pnpm build` and other heavy build commands, and a genuine fresh-clone check requires them. The most recent verification run (`artifacts/verification/latest/report.json`, gitSha `3ee4069`, one commit behind current `HEAD`) shows `lint`, `typecheck`, `test:unit`, `test:coverage`, `test:integration`, `test:contract`, `test:e2e` all passing in place, and `artifacts/verification/release-latest/report.json` shows a real `docker build -t sift-release-check .` passing at gitSha `1833f483` — strong circumstantial support, but neither is a literal fresh-clone repro at current `HEAD`, so this stays unticked rather than asserted.
- [x] Repository visibility and license are verified while signed out. — the `gh repo view` and `curl` checks above were made without a GitHub session cookie in this environment (an unauthenticated API/HTTP call). Not verified through an actual browser-based signed-out session in this pass.

## Required text description

The drafted copy lives in `docs/submissions/webmcp/submission-details.md` ("Recommended Sift positioning" and "About the project — paste-ready Devpost copy"). It has not been pasted into Devpost yet — that action is tracked under "Devpost project fields" below. The checks here verify the drafted content itself.

- [x] The description states what Sift does and who it serves. — the "One-line summary" states what it does ("Sift turns a WebMCP-enabled decision workspace into a shared control surface..."); the audience is named consistently as a household — the "Sift proof to foreground" row for Potential Impact and `docs/specs/product.md` line 82 both frame it as "a household actively comparing real car candidates and dealer offers," and the demo script's "Who is on camera" section grounds it in one specific household (married, one child, one due).
- [x] The description explains why the car-purchase use case fits WebMCP. — the "Why WebMCP is essential" section directly: "the page exposes explicit shared attention, typed mutations, source submission, run receipts, and current case context. A spoken preference can therefore redirect an already-running backend investigation and remain visibly synchronized with the page."
- [x] The description explains how WebMCP improves the experience compared with ordinary chat or visual browser automation. — same section: "Without WebMCP, ChatGPT would have to infer page state, ask the user to repeat selected candidates, or manipulate visual controls indirectly" — naming both DOM-inference/automation and repeat-yourself chat as the alternatives it improves on.
- [x] The description explains what the person and agent can do together that was difficult before. — the "What it does" bullet list: read the exact case state together, drive the interface, add/reweight criteria, define a new comparison dimension mid-conversation, populate it with sourced values, request bounded investigation — each framed as a joint capability.
- [x] The description explains how WebMCP is implemented in the deployed page. — "How we built it": "The application registers 26 bounded, schema-validated tools through `document.modelContext`... WebMCP calls and visible interface controls use the same command implementation and mutate the same canonical case state," naming the real mechanism, not just the API name.
- [x] The description names the shared-attention, typed-mutation, source-intake, investigation-request, and run-correlation capabilities actually demonstrated. — the judging-criteria table's "Sift proof to foreground" row for WebMCP Leverage names four of five almost verbatim: "Current page selection, shared UI/tool commands, typed custom concern, **source intake**, **active-run correlation**, and a visible Strands replan caused by a WebMCP command" (source-intake and run-correlation appear as exact phrases; shared attention, typed mutation, and investigation-request are covered by the other three clauses).
- [x] The description does not claim an untested client, integration, or deployment. — checked directly: the "Built-with draft" lists "Amazon Bedrock AgentCore, only if actually deployed" (conditional); "How we built it" states plainly "The WebMCP implementation has been tested in Chrome 152 using its native `document.modelContext` support," which is true and does not claim a ChatGPT session that never happened. No unconditional claim of an untested surface was found by reading the full draft.

## Required public demo video

**Video-blocked, not an internal gap.** `artifacts/demo/sift-webmcp-demo-FINAL.mp4` exists (7,425,197 bytes, confirmed by `ls -la`) and `ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1` on it reports `173.500000` seconds (2:53.5), under the 3:00 hard limit enforced by `scripts/test-submission.ts` (`SIFT_WEBMCP_VIDEO_PATH`, default `docs/demo/webmcp-recording.mp4` — note this default path does **not** point at the actual file's location under `artifacts/demo/`, which is itself why `docs/submissions/release-metadata.json`'s `videoDurationsSeconds.webmcp` still reads `null`: the automated checker is looking in the wrong place, not that no video exists). It has not been uploaded to YouTube and no public URL exists. `docs/submissions/webmcp/demo-script.md` (a complete, cross-checked shot list) is what it was recorded from, but a recorded file is not a published, judge-viewable video.

- [ ] The video is published publicly on YouTube. — blocked; no upload has occurred.
- [ ] The final runtime is strictly under three minutes. — blocked (depends on the actual published/final video, per this audit's rules); the recorded file's own measured duration (173.5s) is under the limit, but that is groundwork, not a published-video verification.
- [ ] The video includes clear spoken audio explaining Sift and its use of WebMCP. — blocked; scripted throughout `demo-script.md`.
- [ ] The working deployed product appears within the first 15 seconds. — blocked; scripted as Beat 1 (0:00–0:15).
- [ ] The video shows live WebMCP-driven interaction rather than only direct UI clicks. — blocked; scripted as Beat 2, and the underlying capability is independently proven live by the host-acceptance session (not merely scripted).
- [ ] The video removes setup, loading, dead time, and irrelevant architecture detail. — blocked; a property of the edit, not yet performed on a published file.
- [ ] The video shows the selected vehicle shared with ChatGPT. — blocked; scripted as Beat 2 ("show `sift_get_case_context` read the exact selection").
- [ ] The video shows a previously unspecified household concern added or reweighted through WebMCP. — blocked; scripted as Beat 3 (`custom.dog_crate_fit`), independently proven at the mechanism level by `apps/agent/src/runtime/run-plan.test.ts`'s `reason: 'new_concern'` revision tests.
- [ ] The video shows that command redirecting an active Strands investigation. — blocked; scripted as Beat 4.
- [ ] The video shows the visible case state, evidence obligations, and recommendation changing causally. — blocked; scripted as Beat 5, independently proven by the car-purchase scenario report's `obligation_status`/`readiness`/`recommendation` assertions (`artifacts/verification/scenarios/car-purchase/assertion-report.json`, 39/39 passing).
- [ ] The video shows an honest unknown and a test-drive question rather than fabricated subjective evidence. — blocked; scripted as Beat 5.
- [ ] The video proves that final shortlist approval remains human-only. — blocked; scripted as Beat 6, independently proven by the scenario report's `human_action` (`approve_proposal:candidate-crv+candidate-outback`) and `forbidden_event_absent` ("no proposal was ever approved with actor \"agent\"") assertions.
- [ ] The video briefly shows correlated Runtime Inspector or automated verification evidence. — blocked; scripted as Beat 7.
- [ ] The public YouTube link, audio, captions, resolution, and playback are verified while signed out. — blocked; no public link exists to verify.

## Devpost project fields

Every item below is a literal Devpost web-form field. No submission has been sent, and no MCP/API tool in this environment can read or write Devpost's form, so none of these can be machine-verified — each is a submitter action. Drafted, ready-to-paste content exists for most fields in `submission-details.md`; pasting it is what remains.

- [ ] Global project title is complete and consistent with the repository and video. — submitter action; draft title "Sift" is in `submission-details.md`.
- [ ] Global project tagline is complete. — submitter action; draft one-line summary is in `submission-details.md`.
- [ ] Global project description is complete. — submitter action; draft content verified complete against all seven content requirements above (see "Required text description").
- [ ] Global Built With list is truthful and complete. — submitter action; draft list is in `submission-details.md` "Built-with draft," headed by WebMCP/`document.modelContext` and including Strands Agents SDK, with AgentCore explicitly marked conditional.
- [ ] Global public video URL points to the final under-three-minute YouTube video. — submitter action; blocked on the video being published (see "Required public demo video").
- [ ] Field `28249` — Submitter Type is answered truthfully. — submitter action.
- [ ] Field `28250` — Country of residence for the submitter and team is answered truthfully. — submitter action.
- [ ] Field `28251` — Organization name is completed if applicable. — submitter action.
- [ ] Field `28252` — App Status is answered truthfully as New or Existing. — submitter action; repository history supports `New` (see "Application qualification" above) — only the act of selecting it on the live form remains.
- [ ] Field `28253` — Existing-app changes are described if and only if required by the App Status answer. — submitter action; not applicable given the expected `New` answer (see "Application qualification" above).
- [ ] Field `28254` — Judge-accessible live URL is the final verified deployment. — submitter action; the URL itself is live and verified in this session (see "Required live application" above), but it currently serves commit `73d60b1`, not current `HEAD` — redeploy before pasting.
- [ ] Field `28255` — Testing instructions or credentials are supplied when useful, even though optional. — submitter action; a draft seven-step, credential-free sequence already exists in `submission-details.md`.
- [ ] Field `28256` — Public repository URL is the final verified repository. — submitter action; the URL itself is verified public with a visible MIT license (see "Required public repository" above) — only the act of entering it remains.
- [ ] Field `28257` — Agents or clients used to test WebMCP lists only completed tests and exact versions/configurations. — submitter action; the one completed, evidenced test is Chrome 152.0.7977.75 with the `--enable-features=WebMCP,WebMCPTesting,DevToolsWebMCPSupport` flags (`docs/submissions/release-metadata.json`'s `webmcpTestClients`) — no ChatGPT session has actually been completed, so listing ChatGPT here would itself violate this field's own "only completed tests" instruction until one is run.
- [ ] Field `28258` — AI tools used while building lists only tools actually used. — submitter action; this repository's own git history (Claude Code co-authorship on nearly every commit) is real evidence of what was used, but compiling and entering the final list is a submitter action.
- [ ] Field `28259` — Learning level is selected personally as None, Moderate, or Significant. — submitter action; a personal, non-machine-verifiable attestation.
- [ ] Field `28260` — Career-reusable AI value is selected personally as Yes or No. — submitter action; a personal, non-machine-verifiable attestation.

## WebMCP functionality proof

- [x] `sift_get_case_context` returns the exact current page selection and case identity. — the live host-acceptance session (`artifacts/host-acceptance/2026-09-02T18-26-35-407Z/report.json`, against the deployed URL) check "sift_get_case_context" reports "host read caseId case-a389fc14-8a2a-4d15-a237-e71f815cea38 at sequence 12," and check "human action visible to host" confirms the same call reflects a page-side click a moment later (`eventSequence` 12 → 13); `apps/web/src/model-context/case-context.ts`'s `selectedOptionId: caseState.selectedOptionId` field mapping (line ~545) is unit-tested in `case-context.test.ts`.
- [x] Criteria and custom concerns can be added through typed WebMCP commands without changing the immutable pack hash. — structurally verified by reading `packages/core/src/reducer.ts` directly: `pack` is assigned only by the `case.created` (line 98) and `case.pack_selected` (line 161) branches of the reducer's switch statement; `criteria.updated` (line 170, what `sift_update_criteria` emits) and every extension/attribute event type touch only their own field, never `pack`. `apps/agent/src/fixtures/apartment-hunt-extension.test.ts` proves the behavioral round trip for a typed `custom.*` concern end to end through SQLite; `apps/agent/src/routes/catalog-case-integration.test.ts` confirms `snapshot.pack.compiledHash` for the `car-purchase` pack specifically is exactly the registry's compiled hash.
- [x] `sift_request_investigation` creates or redirects bounded work and returns a correlated receipt. — `register-sift-tools.ts` (lines ~315-331) sets `envelope.runId = receipt.runId` whenever the underlying `CommandReceipt` carries one; `register-sift-tools.test.ts` asserts `result.runId).toBe('run-9')` for this specific tool; the live host session's check "sift_request_investigation" reports "run accepted; host watched activeRun.runId... and read the finished recommendation: present," proving the correlated receipt against the real deployed backend, not just a unit double.
- [x] Duplicate, stale, unauthorized, and malformed WebMCP commands are rejected deterministically. — all four verified directly: **duplicate** — `apps/agent/src/routes/commands.test.ts` "is idempotent over HTTP: retrying the same Idempotency-Key does not double-apply"; **stale** — `register-sift-tools.test.ts` "maps a CONFLICT rejection and surfaces the latest sequence when the error carries one" (`SiftClientError('Stale expectedSequence.', ...)`); **unauthorized** (scoped to a case that is not the active one) — `register-sift-tools.test.ts` "rejects a caseId that is not the active case, without calling SiftCommands" → `{code: 'NOT_FOUND', retryable: false}`; **malformed** — the same file's "returns VALIDATION and never calls SiftCommands for malformed input," exercised across every tool via `.strict()` Zod schemas. The live host session's check "blind write refused — pass — refused: Input failed validation against the tool schema" corroborates malformed-input rejection against the real deployed page.
- [x] A WebMCP command can steer an already-active backend run and the causal transition is visible in real time. — `apps/agent/src/services/command-service.ts`'s `updateCriteria` (line ~2103) and `defineCaseAttribute` (line ~1435) — both real WebMCP-registered commands (`sift_update_criteria`, `sift_define_case_attribute`) — call `notifyRunPlan(...)`, the identical hook `setCandidateDisposition` uses; `apps/agent/src/routes/run-plan.test.ts` proves this exact wiring live over real HTTP + SQLite: "revises the plan when a person keeps a candidate, and says what survived" takes an already-existing plan (version 1, created by a prior run request) to version 2 with `reusedSignatures` populated, and "narrates the revision in the activity stream a person is watching" confirms `plan.revised` reaches the same polled/SSE activity feed the browser consumes in real time. `apps/agent/src/runtime/run-plan.test.ts` additionally unit-tests a `reason: 'new_concern'` revision (the exact class of trigger a WebMCP-defined custom concern produces).
- [x] Page UI actions and WebMCP commands converge on the same command/state model. — `register-sift-tools.ts` calls the identical `SiftCommands` methods (`commands.updateCriteria`, `commands.upsertOption`, etc.) that visible UI controls call; `tests/e2e/generic-decision-workspace-journey.spec.ts` line 255's own comment states a UI action drives "the exact same `setView` command a `sift_set_view` WebMCP call would," and `webmcp-contract.test.ts`'s "Callback-vs-envelope equivalence" test directly asserts a WebMCP tool call and a direct `SiftCommands` call resolve identical `commandId`/`caseId`/`sequence` fields for the same input.
- [x] Refresh or connection interruption restores current state and ordered event replay. — the live host session's checks "reload persistence" (same `caseId` after reload), "re-registration after reload" (26 tools re-registered), and "host reconnect" (26 tools re-announced to a freshly enabled host) prove this against the real deployed page; `apps/agent/src/routes/events.sse.test.ts` (part of the passing `test:integration` suite) covers ordered replay and `Last-Event-ID` duplicate suppression at the SSE layer generically.
- [x] No registered WebMCP tool can approve the final shortlist or bypass the visible human authority boundary. — `webmcp-contract.test.ts` "no registered tool name has approval/rejection semantics" (regex-checks all 26 names), "`sift_request_revision` has no decision/actor field in its real input schema," and "none of the twenty-six registered tools ever calls the one SiftCommands method that can approve a proposal (`reviewProposal`)" (spies on `reviewProposal`, invokes the closest-shaped tool, asserts it was never called); the live host session's check "no approval tool — pass — the catalog exposes no tool that can approve a decision" confirms the same against the deployed catalog.
- [x] Tool descriptions reveal only the minimum required case context and do not expose chain-of-thought or secrets. — structural, not merely asserted: `apps/web/src/model-context/case-context.ts`'s header comment states "No chain-of-thought, no secrets, and no oversized source bodies enter the projection," and the `CaseContextSummary` type it builds has no field carrying model transcript or private reasoning — there is no such field anywhere in the case domain, since case state is deterministic domain data, not a model transcript. `case-context.test.ts` bounds the research summary and truncates long free text. `sift_get_case_context`'s own pinned tool description says the same: "It omits private model messages and oversized source bodies."
- [ ] All registered tools pass automated contract and end-to-end tests. — overstated as written. The **contract** half is comprehensively true: `webmcp-contract.test.ts` plus `register-sift-tools*.test.ts` (six files, ~159 `describe`/`it` blocks total) exercise all 26 tools' schemas and per-tool success/error behavior. The **end-to-end** half is not true for all 26: `scripts/test-host.ts`, the only suite that drives a real WebMCP host, actually *invokes* only four tools (`sift_list_packs`, `sift_get_case_context` ×3, `sift_add_note` ×2, `sift_request_investigation`) — confirmed by grepping every `call(...)` site in that script. No Playwright spec invokes WebMCP tools at all (`grep -rln "modelContext\|invokeTool" tests/e2e/*.spec.ts` matches only `first-run-guide.spec.ts`, which checks the *unsupported-browser* indicator, not a real invocation). The remaining ~22 tools are proven registered, schema-correct, and unit-tested, but never actually exercised against a real host or in a Playwright end-to-end run.

## Judging-criteria evidence

### WebMCP Leverage

- [x] The demo proves non-trivial use of shared page context, typed commands, and cross-system steering. — the live host-acceptance session against the deployed page (14/14 checks) exercises shared page context (`sift_get_case_context` reading real selection/sequence) and typed commands (`sift_add_note` with schema validation), and the run-plan revision chain above (`sift_update_criteria`/`sift_define_case_attribute` → `notifyRunPlan` → `RunPlanService.revisePlan`, proven live over HTTP+SQLite) is cross-system steering into the Strands-adjacent planning layer, not a CRUD shortcut.
- [x] The repository makes the WebMCP implementation easy for judges to find and understand. — README's dedicated `## WebMCP` section (line 234), a 62,610-byte `docs/specs/webmcp.md` spec, and a single, centrally located, heavily commented `register-sift-tools.ts` as the "single source of truth" (README's own words) for the tool catalog.
- [x] The submission explains why direct chat, DOM inference, or ordinary API calls do not provide the same shared-control experience. — `submission-details.md` "Why WebMCP is essential": "Without WebMCP, ChatGPT would have to infer page state, ask the user to repeat selected candidates, or manipulate visual controls indirectly" — directly naming DOM inference/visual automation and chat-repetition as the inferior alternatives.

### Execution

- [x] The live product completes the hero journey coherently from selection through human approval. — `artifacts/verification/scenarios/car-purchase/assertion-report.json` (39/39 passing) traces the complete loop: `pack_selected` → six specialists/graph nodes → four skill activations → context injection → `ConsequenceGuard` `confirm` intervention → an agent-proposed case extension (`custom.rear_facing_seat_behind_driver`) → five satisfied obligations → `readiness: true` → `recommendation` → three ordered `human_action`s (`focus_option`, `confirm_case_extension`, `approve_proposal`) → `forbidden_event_absent` (no agent-side approval ever occurred).
- [x] The right-pane UI, real-time events, reconnect behavior, debugging view, and deterministic release suite work in production. — right-pane UI: `chatgpt-pane-640`/`right-pane-390/430/480` snapshots pass; real-time events and reconnect: the live host session's reload/re-registration/reconnect checks against the deployed URL; debugging view: `apps/agent/src/config.ts` line 113 defaults `SIFT_DEBUG_ENABLED` to `true`, and `railway variables --service pax-hackathon --environment production` (checked in this session) sets no override, so the Runtime Inspector's API is live in production by default; deterministic release suite: `artifacts/verification/release-latest/report.json` shows `verify`, `test:mutation`, `release:build`, and `release:docker` all passing (only the video-dependent `test:submission` stage fails, see "Browser and release verification" below).
- [x] Judge testing instructions are short, exact, and reproducible. — `submission-details.md`'s "Testing instructions draft" is seven concrete numbered steps; step 3-6 (get case context, define/update criteria, request investigation, refresh) match almost exactly what the real host-acceptance script executed and passed against the live deployment, corroborating reproducibility rather than just readability.

### Potential Impact

- [ ] The submission identifies a credible household actively comparing real vehicle candidates and dealer information. — **overstated as written; leaving unticked as a genuine finding, not a mere gap.** The vehicle candidates are real, published models (RAV4, CR-V, CX-5, Outback, from the EPA-derived `vehicle-catalog.json`), but the "dealer information" this item asks for is explicitly **not real** anywhere in the product: `packages/catalog/data/vehicle-demo-profiles.json`'s own top-level `$comment` states "Nothing here is a dealer listing: there is no local availability, no current price, and no specific vehicle" and labels every record `curated_demo`; `docs/submissions/webmcp/claim-evidence-matrix.md` row E3 lists "Listings, prices, dealers, or availability are real" as a claim Sift *deliberately does not make*; and the demo script itself narrates the one dealer offer used in the hero flow as "Dealer written offer **(fictional)**" (`demo-script.md` line 237). This is a structural product decision (curated demo data, honestly labeled), not a bug to fix — but it means this checklist item's literal wording cannot be ticked without asserting something the codebase explicitly disclaims.
- [x] The demonstrated workflow addresses changing preferences, conflicting evidence, and subjective unknowns. — the car-purchase scenario report shows a criteria reweight, an agent-proposed custom concern requiring human confirmation, an obligation left `accepted_uncertainty` (not silently resolved), and `docs/submissions/webmcp/claim-evidence-matrix.md` row A7 ("Sift says what it does not know instead of filling the gap") with its own automated proof (`run-plan.test.ts` "records a concern no pack capability can answer as an explicit unknown").
- [x] The outcome is an evidence-backed shortlist and next-action plan rather than generic purchasing advice. — the scenario report's `recommendation` assertion names a specific favored candidate with per-obligation status; `sift_explain_ranking`'s pinned description (`webmcp-contract.test.ts`) guarantees a per-criterion breakdown, not a generic verdict; `claim-evidence-matrix.md` row C3 ("The next step is never empty") is backed by `buildDecisionOrientation`'s `FALLBACK_NEXT_STEP`, tested in `decision-orientation.test.ts`.

### Creativity and Ambition

- [x] The submission clearly distinguishes Sift from CRUD tool wrappers and browser macros. — `submission-details.md` "Distinguishing claim": "Most WebMCP examples let an agent operate a website. Sift lets a website mediate collaboration among a human, ChatGPT, and a separate supervised agent team — and keeps the analysis on the website's side of that boundary."
- [x] The demo proves that WebMCP is a live steering channel into a supervised multi-agent system. — the same evidence chain as "A WebMCP command can steer an already-active backend run" above: `sift_update_criteria`/`sift_define_case_attribute` reach `notifyRunPlan` → `RunPlanService.revisePlan`, proven live over real HTTP + SQLite, feeding the same Strands Graph investigation the car-purchase scenario report exercises end to end.
- [x] The human, ChatGPT, page, and Strands runtime each have a distinct, necessary role. — `submission-details.md` "How we built it" states this as an explicit, five-line architecture list: "WebMCP gives the assistant structured access... The assistant interprets intent... Sift's deterministic core owns case state, ranking, evidence rules, and readiness... The Strands runtime performs bounded multi-agent investigation... The person retains authority over consequential decisions" — matching the actual code boundary (`register-sift-tools.ts` vs. `packages/core` vs. `apps/agent/src/runtime` vs. the human-only `reviewProposal` path).

## Browser and release verification

- [x] Every registered tool is tested end to end in ChatGPT's in-app browser; if access prevents this, the limitation is stated exactly. — the "otherwise" branch is what applies here, and it is stated with precision, not glossed over: `docs/submissions/webmcp/claim-evidence-matrix.md` row E6 ("A real **host** session has [run]... That is a browser, not that product") and row E7 ("no model chose anything... it proves nothing about a model's unaided use of it"), plus `host-acceptance.md`'s "What still needs a person" table, which lists the exact five remaining steps (open in that product, ask what tools it offers, ask it to work the case, ask it to confirm the shortlist — expected to fail — and record the transcript). This mirrors the same honest-limitation pattern the sibling Agents-for-Humans checklist ticks for its own AgentCore item.
- [ ] Every registered tool is tested in the supported Chrome configuration and the exact version/flag is recorded. — genuinely not true for "every registered tool." The exact version/flag half is true (`docs/submissions/release-metadata.json`'s `webmcpTestClients`: Chrome `152.0.7977.75`, `--enable-features=WebMCP,WebMCPTesting,DevToolsWebMCPSupport`), but as established under "WebMCP functionality proof" above, `scripts/test-host.ts` actually *invokes* only 4 of the 26 registered tools against that real Chrome session; the other 22 are proven registered and schema-correct (via the live "discovery" check and the separate jsdom-based `webmcp-contract.test.ts`), not individually functionally tested in that Chrome configuration.
- [x] `pnpm test:contract` passes the WebMCP tool and authority-boundary contracts against the production-compatible build. — `artifacts/verification/latest/report.json`'s `test:contract` stage (`vitest run apps/web/src/model-context/webmcp-contract.test.ts apps/agent/src/routes/agentcore.test.ts`, per `package.json`) shows `status: "passed"`, `exitCode: 0`.
- [x] `pnpm test:e2e` passes the WebMCP journey at every required narrow right-pane viewport. — verified by inspection of existing evidence rather than re-running the suite (this audit excludes `pnpm test:e2e` as a heavy command): `artifacts/verification/latest/report.json`'s `test:e2e` stage passed; `docs/build-log.md`'s 2026-09-05 entry independently records "`npx playwright test` → 192 passed across six viewports, three consecutive clean runs"; `tests/e2e/car-purchase-journey.spec.ts-snapshots/` contains 36 PNGs (6 named states × 6 viewport projects), including all four required (`right-pane-390`, `right-pane-430`, `right-pane-480`, `desktop-1440`) plus two extra (`chatgpt-pane-640`, `expanded-820`).
- [x] The Playwright visual stage passes with reviewed screenshots for the complete WebMCP hero journey. — same evidence as above; `docs/submissions/release-metadata.json`'s `screenshotInventory` records the baselines were "Visually inspected as a set on 2026-08-28: legible and structurally sound at all viewports... no overlap/overflow observed," and `docs/build-log.md`'s 2026-09-05 entry documents a subsequent full-suite pass (192/192) after unrelated Home Energy fixes, with baselines "regenerated only after each cause was fixed, and only for content that genuinely changed" (i.e., not touched for the WebMCP journey, which had no defect in that pass).
- [ ] `pnpm test:submission` validates every machine-checkable WebMCP artifact and field. — **currently fails.** `artifacts/verification/release-latest/report.json` (gitSha `1833f483`, itself several commits behind current `HEAD`) shows `test:submission` failing on exactly two checks: `release-verification-sha` (the latest `verify` report's gitSha no longer matches `HEAD` — expected to clear once the in-flight background `pnpm verify` run at current `HEAD` completes) and `release-metadata-public-urls` (`webmcpVideoUrl` and `agentsForHumansVideoUrl` are unset — the video-publication blocker). Both `scenario-report:car-purchase` (39/39) and `scenario-report:home-energy-guardian` (33/33) checks pass.
- [ ] `pnpm verify:release` passes against the final public repository and live URL. — **currently fails**, same report: `status: "failed"`, with `verify`, `test:mutation`, `release:build`, and `release:docker` all passing and only `test:submission` failing, for the identical two reasons above. Not a diffuse set of defects, but honestly not a passing run as of this writing — and the report itself is now stale (gitSha `1833f483`, three commits behind current `HEAD`).
- [ ] Final URLs, commit SHA, test clients, browser versions, video duration, and report path are recorded in release metadata. — partially true, left unticked because the compound claim is not fully satisfied: `docs/submissions/release-metadata.json` records `repositoryUrl`, `deployedUrl`, `submittedCommitSha` (explicitly marked as needing a refresh before tagging), the real Chrome test client with exact version/flags, and `reportPaths`. **Video duration is not recorded** — `videoDurationsSeconds.webmcp` is still `null` even though the actual video file now exists (`artifacts/demo/sift-webmcp-demo-FINAL.mp4`, 173.5s), because the automated duration checker looks for a hardcoded path (`docs/demo/webmcp-recording.mp4`) that the real file was never placed at or symlinked to — a concrete, fixable gap distinct from the video-publication blocker.

## Submit and preserve

- [ ] A human compares the final Devpost form with every checkbox above. — submitter action; the explicit final human gate this checklist exists to feed.
- [ ] The submission is completed before September 3, 2026 at 1:00 p.m. PT / 4:00 p.m. ET. — **already false, not merely open.** The current session clock reads `2026-09-05` (`date` → `Sat Sep 5 ... 2026`), roughly two days after the `2026-09-03T20:00:00Z` deadline, and no submission has been sent. This item cannot become true by any further work in this repository; it is recorded here plainly rather than left to look like an ordinary open checkbox.
- [ ] Devpost shows the project as submitted rather than saved as a draft. — submitter action; moot until the deadline question above is resolved with the actual competition organizers.
- [ ] Submission confirmation is saved privately. — submitter action.
- [ ] The submitted repository commit receives an immutable release tag. — submitter action; `git tag` in this session returns no tags, confirming this has genuinely not happened.
- [ ] The repository, live site, form, and video are frozen during judging unless the official rules permit a change. — submitter action; not applicable until submission occurs.
