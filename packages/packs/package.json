{
  "name": "@pax/packs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pax Decision Pack compiler, registry, and built-in manifests. Real logic ships in Task 3.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  }
}
