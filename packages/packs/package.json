{
  "name": "@sift/packs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sift Decision Pack compiler, registry, and conformance runner, plus the built-in car-purchase and home-energy-guardian manifests.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@sift/contracts": "workspace:*",
    "@sift/core": "workspace:*"
  },
  "devDependencies": {
    "fast-check": "^4.9.0"
  }
}
