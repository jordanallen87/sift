{
  "name": "@sift/core",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "Pure Sift case reducer, routing, obligations, evidence, readiness, and deterministic scoring engine. No React, Express, Strands, model provider, or filesystem imports.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@sift/contracts": "workspace:*"
  },
  "devDependencies": {
    "fast-check": "^4.9.0"
  }
}
