{
  "name": "@sift/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure Sift case reducer, routing, obligations, evidence, and readiness engine. No React, Express, Strands, model provider, or filesystem imports. Real logic ships in Task 2.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@sift/contracts": "workspace:*"
  },
  "devDependencies": {
    "fast-check": "^4.9.0"
  }
}
