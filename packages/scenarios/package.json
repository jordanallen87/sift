{
  "name": "@pax/scenarios",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pax fixture data, scripted tools/model, scenario runner, and assertions. Fixture tools ship in Task 3; scripted model/runner/assertions ship in later tasks.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@pax/contracts": "workspace:*",
    "zod": "^4.4.3"
  }
}
