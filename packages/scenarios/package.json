{
  "name": "@sift/scenarios",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "Sift fixture data, scripted tools and model provider, scenario runner, and trajectory assertions.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@sift/contracts": "workspace:*",
    "@sift/core": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@sift/packs": "workspace:*"
  }
}
