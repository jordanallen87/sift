# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo

# Local data and sessions
.pax-data/
.strands/

# Generated verification/test artifacts
artifacts/
coverage/
playwright-report/
test-results/
.playwright-mcp/
.stryker-tmp/
reports/mutation/

# Env and secrets
.env
.env.local
.env.*.local
!.env.example

# Editor and OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Local tool state (Strata19 project index, not source)
.strata19/

# Local Claude Code session state, not source
.claude/

# Docker
.docker/

# Local runtime database (CLAUDE.md: `.sift-data/sift.sqlite` locally, `/data` on Railway).
# Runtime state, not a fixture -- it was tracked, so every local run dirtied the tree.
.sift-data/

# Agent-tooling operating instructions and the internal engineering log.
# Kept on disk (they are this project's own working record) but deliberately
# not published: they are build-process scaffolding, not part of the product
# a reader of this repository needs.
CLAUDE.md
CLAUDE_CODE_PROMPT.md
docs/final-plan/claude-code-implementation-prompt.md
docs/build-log.md
